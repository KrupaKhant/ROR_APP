
<style>
table, th, td {
  border: 1px solid black;
  border-collapse: collapse;
}
th, td {
  padding: 15px;
}
</style>

<h1>List of Photos</h1>

<table style="border: 1px solid black">
<thead >
    <tr>
        <th>Title</th>
        <th>Description</th>
        <th>Action</th>
        <th>Action</th>
    </tr>
</thead>
<tbody>
    
    <% @photos.each do |photo|%>

        <tr>
        <td><%= photo.title%></td>
        <td><%= photo.description%></td>
        <td><%= link_to "Edit", edit_photo_path(photo) %></td>
        <td><%= link_to "Delete", photo_path(photo), data: {turbo_method: :delete} %></td>
        </tr>

    <% end %>
</tbody>

</table>

<p><%= button_to "New Photo", new_photo_path, method: :get %></p>